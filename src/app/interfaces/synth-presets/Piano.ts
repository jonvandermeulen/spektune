// import * as Tone from 'Tone';
// import { Piano } from '@tonejs/piano';

// export class PianoSim extends Piano {

//     triggerAttackAndRelease(note: Tone.Unit.Frequency, 
//                             duration: Tone.Unit.Time | Tone.Unit.Time[], 
//                             time?: Tone.Unit.Time, 
//                             velocity?: Tone.Unit.NormalRange): this {
//         this.keyDown({ note: note.valueOf(), time });
//         this.keyUp({ note: note.note, time: time + Tone.Time(note.duration).valueOf() });
//         return null;
//     }
// }
